{
  "name": "codebase-explainer",
  "version": "1.0.0",
  "description": "Full-stack MERN application to analyze GitHub repositories using AI",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ..",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "npx kill-port 5001 3000 && concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
